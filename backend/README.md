# Backend Structure

This folder is intentionally scaffolded only.

- `app/api/v1/` for FastAPI/Litestar routes
- `app/core/` for settings and security
- `app/services/` for business logic and AI verification pipeline
- `app/models/` for ORM and schemas
- `tests/` for backend tests
